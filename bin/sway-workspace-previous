#!/bin/sh

# Switches to the previous workspace.
# If the workspace does not exist, a new workspace will be created.
#
# Usage:
#
# sway-workspace-previous

swaymsg workspace $(swaymsg -t get_workspaces | jq '.[] | select(.focused).num - 1')
