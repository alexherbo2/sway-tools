#!/bin/sh

sway-get-windows-with-workspace | jq '
  [
    {
      key: "[\(.workspace.name)] [\(.window.name)] [\(.window.marks | join(" "))] [\(.window.id)]",
      value: .
    }
  ] | from_entries
'
